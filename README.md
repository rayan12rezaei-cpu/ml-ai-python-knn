# ML ( AI ) in Python
ML ( simple AI ) in python ( KNN [ K-Nearest Neighbors ] model )

MLs (Machine Learning) are kinds of advanced algorithms that can predict some features with the data that are given to them.
Python is a very good language for ML and AI.

In this project we're going to learn how to code a simple KNN model in python.

Models of ML can be **regressor** or **classifier**

We're going to use ...
- Python
- CSV
- ML: _KNN_ + _Regression_


dependencies:
- Python3
- pip3
- scikit-learn
- pandas

After you installed Python3 and pip3 ...

```pip install sklearn```

for install scikit-learn to get started with ML

pandas:

```pip install pandas```
